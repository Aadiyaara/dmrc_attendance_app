import 'package:flutter/material.dart';

class Supervisor extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      child: Center(
        child: Text('Supervisor UI'),
      ),
    );
  }
}